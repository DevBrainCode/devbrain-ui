let selector = '.my-custom-switch';
let trigger = document.querySelector( selector );
let customSwitchNode = new SwitchComponent( trigger );

customSwitchNode.onAfterChange = () => console.log( customSwitchNode.isActive() );