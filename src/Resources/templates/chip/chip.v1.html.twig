{# ------------------ DevBrain Component : Chip - Settings ------------------ #}
{# -------------------------------------------------------------------------- #}

{% set _class = class|default('') %}



{# -------------------- DevBrain Component : Chip - Attr -------------------- #}
{# -------------------------------------------------------------------------- #}

{% set _attr = attr|default({}) %}

{# Class Attribute #}
{% set _attr = _attr|merge({class: ''}) %}
{% set _attr = _add_attribute(_attr, 'class', _attr.class ~' chip') %}
{% set _attr = _add_attribute(_attr, 'class', _attr.class ~' ' ~ _class) %}



{# ------------------ DevBrain Component : Chip - Content ------------------- #}
{# -------------------------------------------------------------------------- #}
<div {{ _get_attributes(_attr) }}>
    Chip
</div>